<h1>HOME PAGE BOYZ</h1>

<% @users.each do |user| %>
<div>
	<%= user.name %>
</div>
<% end %>

		<%= link_to 'Yo albums', albums_path %>



<% if logged_in? %>
		<h2>YOUR NAME IS <%= current_user.name %></h2>
		<%= link_to 'Log Out', logout_path, method: :delete %>
		<%= link_to 'Yo albums', albums_path %>
	<% else %>
		<%= link_to "SignUp", new_user_path %>
		<%= link_to 'Log In', login_path  %>

<% end %>